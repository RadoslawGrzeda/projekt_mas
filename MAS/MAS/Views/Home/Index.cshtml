@* <!DOCTYPE html>
<html>
<head>
    <title>Test Reservation</title>
</head>
<body>
    <h2>Make Reservation</h2>
    <form id="reservationForm">
        <label>Car ID: <input type="number" id="carId" required></label><br>
        <label>Start Date: <input type="date" id="startDate" required></label><br>
        <label>Days: <input type="number" id="days" required></label><br>
        <button type="submit">Reserve</button>
    </form>

    <pre id="result"></pre>

    <script>
        document.getElementById("reservationForm").addEventListener("submit", async (e) => {
            e.preventDefault();

            const data = {
                carId: parseInt(document.getElementById("carId").value),
                startDate: document.getElementById("startDate").value,
                numberOfDays: parseInt(document.getElementById("days").value)
            };

            const response = await fetch("https://localhost:7034/api/reservation/make_reservation", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(data)
            });

            document.getElementById("result").textContent = await response.text();
        });
    </script>
</body>
</html> *@
@{
    ViewData["Title"] = "Home Page";
}

<h1>Wypożyczalnia Samochodów</h1>
<p>Witaj w systemie rezerwacji!</p>

<a asp-controller="Reservation" asp-action="Create">Zrób rezerwację</a>